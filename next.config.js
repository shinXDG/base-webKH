/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  staticPageGenerationTimeout: 90, // time in seconds of no pages generating during static. Generation before timing out
};
